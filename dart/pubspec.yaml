name: gta_colors
description: Shared GTA color name <-> hex table (mirrors the @gta/colors TypeScript package).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.25.0
